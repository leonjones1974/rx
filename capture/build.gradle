
dependencies {
    compile 'io.reactivex:rxjava:1.0.8'
    compile 'com.google.guava:guava:18.0'
    compile 'javassist:javassist:3.12.1.GA'
}

jar {
    manifest {
        attributes(
                "Premain-Class": 'com.cam.rx.capture.instr.CaptureAgent'
        )
    }
}

